# API Keys
# Replace these with your actual keys in .env.local (which should not be committed)

# Public keys (exposed to browser)
NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=your-ga-id-here

# Server-only keys (not exposed to browser)
EMAIL_SERVICE_API_KEY=your-email-api-key-here

# Any other environment variables your app needs
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret 